""" AppConf for learn.users """

from django.apps import AppConfig


# Create your config here.
class ItemsConfig(AppConfig):
    """App configuration for learn.users"""

    name = "learn.users"
    default_auto_field = "django.db.models.BigAutoField"
