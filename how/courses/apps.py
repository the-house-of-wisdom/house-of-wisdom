"""AppConf for how.courses"""

from django.apps import AppConfig


# Create your config here.
class CoursesConfig(AppConfig):
    """App configuration for how.courses"""

    name = "how.courses"
    default_auto_field = "django.db.models.BigAutoField"
