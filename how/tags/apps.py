"""AppConf for how.tags"""

from django.apps import AppConfig


# Create your config here.
class TagsConfig(AppConfig):
    """App configuration for how.tags"""

    name = "how.tags"
    default_auto_field = "django.db.models.BigAutoField"
