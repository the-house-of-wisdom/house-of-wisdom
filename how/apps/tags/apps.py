"""AppConf for how.apps.tags"""

from django.apps import AppConfig


# Create your config here.
class TagsConfig(AppConfig):
    """App configuration for how.apps.tags"""

    name = "how.apps.tags"
    default_auto_field = "django.db.models.BigAutoField"
